version: "3.1"

services:
  server:
    user: root
    env_file:
      - .env
      #- .env.local
    entrypoint: ['/bin/sh', '-c', "./scripts/gag.sh" ]
    volumes:
      - ./:/app/
    ports:
      - "${ZENBOT_PORT_OUT}:17365"
      #- "9229:9229"
